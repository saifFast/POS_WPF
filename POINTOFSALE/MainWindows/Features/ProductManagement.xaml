<Window x:Class="POINTOFSALE.MainWindows.Features.ProductManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:POINTOFSALE.MainWindows.Features"
        mc:Ignorable="d"
        Title="ProductManagement" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Row for buttons -->
            <RowDefinition Height="*"/>
            <!-- Row for DataGrid -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Buttons -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Button Content="Add Product" Width="120" Height="40" Margin="10,0" />
            <Button Content="Update Product" Width="120" Height="40" Margin="10,0" />
            <Button Content="Delete Product" Width="120" Height="40" Margin="10,0" />
            <Button Content="List Products" Width="120" Height="40" Margin="10,0" Click="ListProducts_Click" />
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="ProductsDataGrid" Grid.Row="1" AutoGenerateColumns="False" HeadersVisibility="Column" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                  Margin="10" Visibility="Collapsed">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                <DataGridTextColumn Header="Category ID" Binding="{Binding CategoryId}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
