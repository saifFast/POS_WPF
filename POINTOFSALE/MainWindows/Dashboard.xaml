<Window x:Class="POINTOFSALE.MainWindows.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:POINTOFSALE.MainWindows"
        mc:Ignorable="d"
        Title="Dashboard" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Row for the title -->
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="DASHBOARD" 
                   FontSize="30" FontWeight="Bold" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Margin="0,10,0,10" />

        <!-- Buttons -->
        <Button Grid.Row="1" Grid.Column="0" Content="Order Management" FontSize="20" />
        <Button Grid.Row="1" Grid.Column="1" Content="Product Management" FontSize="20" Click="Button_Click" />
        <Button Grid.Row="1" Grid.Column="2" Content="User Management" FontSize="20" />
    </Grid>
</Window>